# 🚀 Replit + Vercel 混合部署指南

## 架构设计

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│   用户界面   │────▶│ Vercel API   │────▶│Replit 服务  │
│  (Vercel)   │     │ (主逻辑)     │     │(视频处理)   │
└─────────────┘     └──────────────┘     └─────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  AI APIs     │
                    │(MiniMax等)   │
                    └──────────────┘
```

## 第一步：部署 Replit 视频服务

### 1. 在 Replit 创建项目

1. 登录 [Replit](https://replit.com)
2. 点击 "+ Create"
3. 选择 "Python" 模板
4. 命名为 "video-processor-service"

### 2. 上传代码

将以下文件复制到 Replit：
- `replit-video-service/main.py`
- `replit-video-service/requirements.txt`
- `replit-video-service/.replit`
- `replit-video-service/replit.nix`

### 3. 运行服务

点击 "Run" 按钮，Replit 会：
- 安装 Python 包
- 安装 FFmpeg
- 启动 Flask 服务

### 4. 获取服务 URL

服务启动后，你会看到类似这样的 URL：
```
https://video-processor-service.你的用户名.repl.co
```

## 第二步：配置 Vercel 环境变量

### 1. 打开 Vercel Dashboard

访问 https://vercel.com/dashboard

### 2. 进入项目设置

选择 "jiaoben" 项目 → Settings → Environment Variables

### 3. 添加 Replit 服务 URL

添加新的环境变量：
- Key: `REPLIT_VIDEO_SERVICE_URL`
- Value: `https://video-processor-service.你的用户名.repl.co`
- Environment: Production, Preview, Development

### 4. 重新部署

环境变量添加后，需要重新部署才能生效。

## 第三步：测试完整流程

### 1. 测试 Replit 服务

```bash
# 健康检查
curl https://你的服务.repl.co/health

# 应该返回
{
  "status": "healthy",
  "ffmpeg_available": true,
  ...
}
```

### 2. 测试视频处理

访问：https://jiaoben.vercel.app/video-transcribe-dashboard.html

输入视频 URL，系统会：
1. Vercel API 接收请求
2. 调用 Replit 服务下载视频和提取音频
3. 使用 MiniMax API 转写音频
4. 使用 Tongyi API 生成脚本

## 常见问题

### Q: Replit 服务显示 "Waking up..."
A: 免费版 Replit 会在闲置后休眠，首次访问需要等待 5-10 秒唤醒。

### Q: 视频下载失败
A: 可能需要在 Replit 中添加 cookies.txt 文件（从浏览器导出抖音 cookies）

### Q: 超时错误
A: Replit 免费版性能有限，处理可能需要 20-30 秒。确保 Vercel 函数超时设置为 60 秒。

## 成本分析

### Replit 免费版限制
- CPU: 0.5 vCPU
- RAM: 512 MB
- 存储: 1 GB
- 月流量: 10 GB
- 适合测试和小规模使用

### 升级建议
如果需要更好的性能：
- Replit Hacker Plan: $7/月
- 更多 CPU 和内存
- 始终在线（无休眠）

## 监控和维护

### 查看 Replit 日志
在 Replit 界面的 Console 标签查看实时日志

### 查看 Vercel 日志
```bash
vercel logs https://jiaoben.vercel.app --follow
```

### 定期清理
Replit 服务包含清理端点，可以设置定时任务：
```bash
curl -X POST https://你的服务.repl.co/cleanup
```

## 完成！

现在你有了一个完整的视频处理解决方案：
- ✅ Vercel 处理 Web 界面和 API 逻辑
- ✅ Replit 处理视频下载和音频提取
- ✅ 真实的 AI API 调用（MiniMax + Tongyi）
- ✅ 完整的 Story 0.2 功能实现