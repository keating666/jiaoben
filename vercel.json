{
  "version": 2,
  "name": "jiaoben-tech-validation",
  "buildCommand": "cd tech-validation && npm install && npm run build",
  "outputDirectory": "tech-validation/dist",
  "installCommand": "cd tech-validation && npm install",
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  },
  "build": {
    "env": {
      "NODE_VERSION": "18.x"
    }
  },
  "regions": ["sin1"],
  "github": {
    "enabled": true,
    "autoAlias": true
  }
}